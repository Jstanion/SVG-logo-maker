// Imports CLI class constructor
const CLI = require('./lib/cli');

// Decalred variable cli to store value of instantiating a new CLI class
const cli = new CLI();

// Function expression
const run = () => {};

// Function called to run application
run(cli);